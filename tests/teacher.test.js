import db from '../utils/db.js';
import watchListService from "../services/watch_list.service.js";
import enrollmentsService from "../services/enrollments.service.js";
import userService from "../services/user.service.js";
import coursesService from "../services/courses.service.js";

(async () => {
    // 1
    // Tạo Course, Section, Lecture đã có
    // Tạo course trong coursesService, tạo section trong sectionService, tạo lectureService

    // 2 update đã có tương tự

    // 3
    // const result = await userService.updateInfo('id', 'path', 'firstname', 'lastname', 'caption', 'biography')

    // 4
    // const result = await coursesService.getByCreateBy('d172436b-5020-4b34-8827-6ebd041d5474')
    // console.log(result);
})();
